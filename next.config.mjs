/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['lh3.googleusercontent.com'],
    loader: 'default', // Optional: Use 'imgix' if you have an imgix account
  },
};

 

export default nextConfig;
